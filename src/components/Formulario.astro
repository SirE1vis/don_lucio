---
const mainImage = {
  webp: "/images/formulario.webp",
  jpg: "/images/formulario.jpg",
  alt: "Imagen de contacto"
};
---

<section class="w-full py-8">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row gap-8 items-start ">
      {/* Left Container - Form */}
      <div class="w-full md:w-1/2 px-10">
        <div class="max-w-full p-12 mx-auto shadow-xl rounded-2xl">
          <h2 class="text-3xl md:text-4xl font-bold text-amber-900 mb-4">
            Envíanos un Mensaje
          </h2>
          <p class="text-gray-600 mb-8">
            Estamos aquí para responder tus consultas
          </p>

          {/* Information Cards */}
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-8">
            <div class="bg-amber-400/50 p-4 rounded-lg shadow-sm text-center">
              <div class="mb-2">
                <span class="text-amber-900 text-2xl">✉️</span>
              </div>
              <h3 class="font-semibold text-gray-800">Escríbenos</h3>
              <p class="text-sm text-gray-600">Responderemos lo antes posible</p>
            </div>

            <div class="bg-amber-400/50 p-4 rounded-lg shadow-sm text-center">
              <div class="mb-2">
                <span class="text-amber-900 text-2xl">🕒</span>
              </div>
              <h3 class="font-semibold text-gray-800">Horario de Atención</h3>
              <p class="text-sm text-gray-600">Lun - Vie: 9:00 AM - 6:00 PM</p>
            </div>

            <div class="bg-amber-400/50 p-4 rounded-lg shadow-sm text-center">
              <div class="mb-2">
                <span class="text-amber-900 text-2xl">📍</span>
              </div>
              <h3 class="font-semibold text-gray-800">Ubicación</h3>
              <p class="text-sm text-gray-600">Lima, Perú</p>
            </div>
          </div>

          {/* Contact Form */}
          <form 
            name="contact" 
            method="POST" 
            data-netlify="true" 
            class="space-y-6"
          >
            <input type="hidden" name="form-name" value="contact" />
            
            <div>
              <input
                type="text"
                name="nombre"
                placeholder="Nombres y Apellidos"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-amber-500 focus:border-transparent"
              />
            </div>

            <div>
              <input
                type="tel"
                name="telefono"
                placeholder="Número de Teléfono"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-amber-500 focus:border-transparent"
              />
            </div>

            <div>
              <textarea
                name="mensaje"
                placeholder="Escribe tu mensaje..."
                rows="6"
                required
                class="w-full min-h-[160px] px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-amber-500 focus:border-transparent resize-none overflow-hidden"
                oninput="this.style.height = 'auto'; this.style.height = this.scrollHeight + 'px';"
              ></textarea>
            </div>

            <div class="w-full flex justify-center">
                <button
                type="submit"
                class=" w-[210px] bg-amber-600 text-white font-medium py-3 px-6 rounded-2xl hover:bg-amber-800 transition-colors duration-300"
                >
                Enviar Mensaje
                </button>
            </div>
          </form>
        </div>
      </div>

      {/* Right Container - Image (hidden on mobile) */}
      <div class="hidden md:block md:w-1/2">
        <div class="relative h-full">
          <picture class="block w-full h-full rounded-xl overflow-hidden">
            <source srcset={mainImage.webp} type="image/webp" />
            <source srcset={mainImage.jpg} type="image/jpg" />
            <img
              src={mainImage.jpg}
              alt={mainImage.alt}
              class="w-full h-full object-cover"
              loading="lazy"
            />
          </picture>
        </div>
      </div>
    </div>
  </div>
</section>